<h1>Cats</h1>
<ul>
  <% @cats.each do |cat| %>
    <li>
      <h3><%= cat.name %></h3>
      <a href="<%= cat_url(cat) %>">Show Me!</a>
    </li>
  <% end %>
</ul>
<br>
<a href="<%= new_cat_url %>">Create Cat!</a>